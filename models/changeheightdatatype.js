'use strict';
module.exports = (sequelize, DataTypes) => {
  var ChangeHeightDatatype = sequelize.define('ChangeHeightDatatype', {
  
  }, {});
  ChangeHeightDatatype.associate = function(models) {
    // associations can be defined here
  };
  return ChangeHeightDatatype;
};