'use strict';
module.exports = (sequelize, DataTypes) => {
  var Student - add - column - height = sequelize.define('Student-add-column-height', {
    height: DataTypes.INTEGER
  }, {});
  Student - add - column - height.associate = function(models) {
    // associations can be defined here
  };
  return Student - add - column - height;
};